"use client";
import {FormEvent,useState} from "react";import {register} from "@/lib/auth";import {useRouter} from "next/navigation";
export default function Register(){const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const router=useRouter();async function submit(e:FormEvent){e.preventDefault();try{await register({display_name:name,email,password});router.push("/login")}catch(e){setError(e instanceof Error?e.message:"Registration failed")}}return <main className="min-h-screen bg-black text-white grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md border border-white/10 rounded-2xl p-8 bg-white/[.03]"><p className="text-neutral-500 text-sm">FORGEOS / ONBOARDING</p><h1 className="text-3xl font-semibold mt-2">Create your account</h1><input className="mt-8 w-full bg-black border border-white/15 rounded-lg p-3" placeholder="Name" value={name} onChange={e=>setName(e.target.value)}/><input className="mt-3 w-full bg-black border border-white/15 rounded-lg p-3" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" className="mt-3 w-full bg-black border border-white/15 rounded-lg p-3" placeholder="Password (10+ characters)" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="text-red-400 text-sm mt-3">{error}</p>}<button className="mt-6 w-full bg-white text-black rounded-lg p-3 font-medium">Create account</button></form></main>}

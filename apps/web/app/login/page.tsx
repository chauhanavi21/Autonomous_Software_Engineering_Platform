"use client";
import {FormEvent,useState} from "react"; import {login} from "@/lib/auth"; import {useRouter} from "next/navigation";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const router=useRouter();async function submit(e:FormEvent){e.preventDefault();try{const data=await login({email,password});sessionStorage.setItem("forgeos_access",data.access_token);router.push("/dashboard")}catch(e){setError(e instanceof Error?e.message:"Login failed")}}return <main className="min-h-screen bg-black text-white grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md border border-white/10 rounded-2xl p-8 bg-white/[.03]"><p className="text-neutral-500 text-sm">FORGEOS / IDENTITY</p><h1 className="text-3xl font-semibold mt-2">Sign in</h1><input className="mt-8 w-full bg-black border border-white/15 rounded-lg p-3" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" className="mt-3 w-full bg-black border border-white/15 rounded-lg p-3" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="text-red-400 text-sm mt-3">{error}</p>}<button className="mt-6 w-full bg-white text-black rounded-lg p-3 font-medium">Continue</button><a href="/register" className="block text-center text-sm text-neutral-400 mt-5">Create an account</a></form></main>}
